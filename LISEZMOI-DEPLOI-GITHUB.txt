Deploiement GitHub Pages

Copiez TOUT le contenu de out/ a la racine du repo blocknums-website.

Le build renomme _next/ en assets/ (Jekyll ignore les dossiers _*).
Ne renommez pas assets/ manuellement.

Optionnel : .nojekyll a la racine (fichier cache sous Windows).

Test apres deploy :
  https://elboz73.github.io/blocknums-website/assets/static/css/
  doit repondre 200 (pas 404).
